Основной класс программы Solution.

InputFromFileUtil  -  класс для чтения строк из входного файла

DefineObjectsInitialDataUtil - В данном классе происходит загрузка объектов из входного списка в список объектов

DefineObjectsParamsUtil  -  В данном классе происходит определение полного набора параметров всех объектов

CreateFileContentUtil - В данном классе создается контент для сохранения в файл

OutputToFileUtil - класс вывода результатов  в файл


На данном этапе не совсем понимаю как реализовать проверку циклического наследования. Есть
предположение, что такого рода проверки реализуются с помощью алгоритмов на графах.

На рисунке "Циклическое наследование.png" представлено приблизительное видение данной проблемы.